<div id="{{codeSnippetName}}">
    <table class="array-report method-table">
        <thead class="method-thead">
        <tr>
            <th class="method-name-column">{{codeSnippetName}}</th>
            {{#each divCodeValues}}
                <th class="right bgwhite">
                    {{#if this.isSelected}}
                        <span class="selected" id="div-file-{{codeSnippetName}}-{{this.metricName}}" onclick="selectOne('{{codeSnippetName}}', '{{this.metricName}}', {{this.metricNamesArray}})">{{this.metricName}}</span>
                    {{else}}
                        <span class="not-selected" id="div-file-{{codeSnippetName}}-{{this.metricName}}" onclick="selectOne('{{codeSnippetName}}', '{{this.metricName}}', {{this.metricNamesArray}})">{{this.metricName}}</span>
                    {{/if}}
                </th>
                <th class="score">{{this.value}}</th>
            {{/each}}
        </tr>
        </thead>
        <tbody class="method-body">
{{#each divCodes}}
    {{>divCode divCode=.}}
{{/each}}
        </tbody>
    </table>
</div>
