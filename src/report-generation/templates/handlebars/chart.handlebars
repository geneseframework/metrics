<script>
    var chartLeftCanvas = document.getElementById('chart-left').getContext("2d");
    <!--var chartLeftCanvas = document.getElementById('chart-{{metricName}}').getContext("2d");-->
    console.log(chartLeftCanvas);
    var barCognitive = new Chart(chartLeftCanvas, {
        type: 'scatter',
        label: '{{metricName}}',
        data: {
            datasets: [{
                data: [
                    {{#each data}}
                        {x: {{this.metricScore}}, y: {{this.measureValue}} },
                    {{/each}}
                ],
                backgroundColor: 'rgb(255, 99, 132)'
            }]
        },
        options: {
            legend: {
                display: false
            },
            scales: {
                xAxes: [{
                    type: 'linear',
                    position: 'bottom',
                    scaleLabel: {
                        display: true,
                        labelString: '{{metricName}}'
                    },
                }],
                yAxes: [{
                    type: 'linear',
                    scaleLabel: {
                        display: true,
                        labelString: 'Time'
                    },
                }],
            }
        }
    });
</script>
