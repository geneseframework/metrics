{{#with rowSnippet}}
    <tr>
        <td><a href="{{linkFile}}">{{fileName}}</a></td>
        {{#if hasMeasure}}
            <td>{{measureValue}}</td>
        {{/if}}
        {{#each scores}}
            <td class="center">{{this}}</td>
        {{/each}}
    </tr>
{{/with}}
