{{#if isDisplayed}}
<tr class="code-row" id="tr-{{codeSnippetName}}-{{metricName}}">
{{else}}
<tr class="code-row not-displayed" id="tr-{{codeSnippetName}}-{{metricName}}">
{{/if}}
    <td>
        <pre>
            <code class="language-js">
{{code}}</code>
        </pre>
    </td>
</tr>
